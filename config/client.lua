---@param useTarget boolean set whether to use target
---@param debugPoly boolean set whether to display a red debug box around the stash location

return {
    useTarget = false,
    debugPoly = false,
}
